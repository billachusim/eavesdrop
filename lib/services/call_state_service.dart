// lib/services/call_state_service.dart

class CallStateService {
  /// The ID of the call the user is currently in.
  /// This is null if the user is not in any call.
  static String? activeCallId;
}
